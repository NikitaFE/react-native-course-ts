import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  productsWrapper: {
    flex: 1,
    paddingHorizontal: 16,
    marginHorizontal: -16,
    overflow: 'hidden',
  },
  productList: {
    overflow: 'visible',
  },
  empty: {
    textAlign: 'center',
  },
});
